<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:background="@drawable/mainp"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/start_quiz"
        android:id="@+id/takeQuizButton"
        android:layout_centerVertical="true"
        android:layout_alignLeft="@+id/quizStatsButton"
        android:layout_alignStart="@+id/quizStatsButton"
        android:layout_alignRight="@+id/quizStatsButton"
        android:layout_alignEnd="@id/quizStatsButton"
        android:focusable="false"
        android:focusableInTouchMode="false"
        android:enabled="true"
        android:backgroundTint="#ff000000"
        android:textColor="#ffffffff" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/quiz_stats"
        android:id="@+id/quizStatsButton"
        android:layout_marginTop="34dp"
        android:layout_below="@+id/takeQuizButton"
        android:layout_centerHorizontal="true"
        android:textColor="#ffffffff"
        android:backgroundTint="#ff000000" />


</RelativeLayout>
